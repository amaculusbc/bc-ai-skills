# [Skill Name]

[What it does in one line.]

## Use it
**Live Gem:** [paste share link, or "not deployed yet"]

## Recreate / update the Gem (maintainers)
1. New Gem → paste `gem.md`.
2. Attach the `uses:` files from `meta.yaml` + anything in `knowledge/`.
3. Update `gem_url` in `meta.yaml`, run `scripts/build_registry.py`.
4. Test against `examples/test-cases.md`.

## Changelog
- 0.1.0 — initial draft.
